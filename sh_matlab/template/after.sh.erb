echo "$(date): Waiting for server to open port ${port}..."

if wait_until_port_used "${host}:${port}" 300; then
  echo "$(date): Discovered server listening on port ${port}!"
else
  echo "$(date): Timed out waiting for server to open port ${port}!"
  clean_up 1
fi

sleep 2
